{
  "name": "herbertph-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Personal QA portfolio — herbertph.com",
  "scripts": {
    "dev": "npx --yes serve . -l 4173",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
